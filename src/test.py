# standard import
import time
import functools
import logging
import scenario
import os


if __name__ == "__main__":
    
    seed = int(os.environ.get("SEED", 1))
    print("hello-world")
    print(seed)